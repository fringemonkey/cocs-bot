# Discord webhook URL for dev/builds channel
# Get this from Discord: Server Settings → Integrations → Webhooks → Create Webhook
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Optional: Secret for validating Cloudflare webhook requests
# If set, webhook requests must include this secret in headers
WEBHOOK_SECRET=
