# Discord Bot Token
# Get this from Discord Developer Portal: https://discord.com/developers/applications
# Create a new application → Bot → Copy token
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Discord Channel ID for dev/builds channel
# Right-click the channel → Copy ID (enable Developer Mode in Discord settings first)
DISCORD_CHANNEL_ID=your_channel_id_here

# Optional: Webhook secret for validating Cloudflare webhook requests
# If set, webhook requests must include this secret in X-Webhook-Secret header
WEBHOOK_SECRET=

# Optional: GitHub repository information for commit links
# Defaults to TLC-Community-Survey/Survey if not set
GITHUB_REPO_OWNER=TLC-Community-Survey
GITHUB_REPO_NAME=Survey
